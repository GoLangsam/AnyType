// Chan{{.}} returns a channel to receive all inputs before close.
func Chan{{.}}(inp ...{{.Dot}}) chan {{.Dot}} {
	out := make(chan {{.Dot}})
	go func() {
		defer close(out)
		for _, i := range inp {
			out <- i
		}
	}()
	return out
}
