// Chan{{.}}Slice returns a channel to receive all inputs before close.
func Chan{{.}}Slice(inp ...[]{{.Dot}}) chan {{.Dot}} {
	out := make(chan {{.Dot}})
	go func() {
		defer close(out)
		for _, in := range inp {
			for _, i := range in {
				out <- i
			}
		}
	}()
	return out
}
